(()=>{if(/admin\.html$/i.test(location.pathname))return;const cfg=window.MARKBEEN5_CONFIG||{};if(!cfg.SUPABASE_URL||!window.supabase)return;const db=supabase.createClient(cfg.SUPABASE_URL,cfg.SUPABASE_PUBLISHABLE_KEY);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));function token(){let t=localStorage.getItem('mb5_voter');if(!t){t=crypto.randomUUID?crypto.randomUUID():Date.now()+'-'+Math.random().toString(36).slice(2);localStorage.setItem('mb5_voter',t)}return t}function shell(){if(document.getElementById('community'))return;const sec=document.createElement('section');sec.id='community';sec.className='community-section section-anchor';sec.innerHTML='<div class="community-head"><div><div class="section-kicker">MB5 COMMUNITY</div><h2>THE GRIND IS BETTER TOGETHER</h2><p class="section-note">Announcements, supporter shoutouts and community votes from MarkBeen5.</p></div></div><div id="communityGrid" class="community-grid"><article class="video-status">Loading community…</article></div><div class="community-join"><a class="btn" href="https://www.twitch.tv/markbeen5" target="_blank" rel="noopener">FOLLOW ON TWITCH</a><a class="btn" href="https://www.youtube.com/@markBeen5" target="_blank" rel="noopener">SUBSCRIBE ON YOUTUBE</a></div>';const about=document.getElementById('about');(about?.parentNode||document.querySelector('main')).insertBefore(sec,about||null);const nav=document.querySelector('.nav-links .admin-link');if(nav){const a=document.createElement('a');a.href='#community';a.textContent='COMMUNITY';nav.before(a)}}async function load(){shell();const g=document.getElementById('communityGrid');if(!g)return;const {data:posts,error}=await db.from('community_posts').select('*').eq('enabled',true).order('sort_order');if(error){g.innerHTML='<article class="video-status">Community is loading soon.</article>';return}const {data:votes}=await db.from('community_votes').select('post_id,option_index');const all=votes||[];g.innerHTML=(posts||[]).map(p=>card(p,all)).join('')||'<article class="video-status">Community updates coming soon.</article>';g.querySelectorAll('[data-vote]').forEach(b=>b.onclick=()=>vote(b.dataset.vote,Number(b.dataset.option)))}function card(p,votes){const pv=votes.filter(v=>v.post_id===p.id),opts=Array.isArray(p.options)?p.options:[];let poll='';if(p.kind==='poll'&&opts.length){const total=pv.length;poll='<div class="poll-options">'+opts.map((o,i)=>{const n=pv.filter(v=>v.option_index===i).length,pc=total?Math.round(n*100/total):0;return `<button class="poll-option" data-vote="${p.id}" data-option="${i}"><span>${esc(o)}</span><b>${pc}%</b><span class="poll-bar"><span style="width:${pc}%"></span></span></button>`}).join('')+'</div><div class="community-status">'+total+' community vote'+(total===1?'':'s')+'</div>'}return `<article class="community-card ${esc(p.kind)}"><div class="community-type">${esc(p.kind).toUpperCase()}</div><h3>${esc(p.title)}</h3>${p.body?`<p>${esc(p.body)}</p>`:''}${poll}</article>`}async function vote(id,i){const {error}=await db.from('community_votes').insert({post_id:id,option_index:i,voter_token:token()});if(error){alert(error.code==='23505'?'You already voted in this poll.':'Vote could not be saved. Please try again.');return}load()}document.addEventListener('DOMContentLoaded',load);setTimeout(load,600)})();