name: Sync Twitch Halloween Highlights

on:
  workflow_dispatch:
  schedule:
    - cron: '*/30 * * * *'
  push:
    paths:
      - '.github/workflows/twitch-halloween-sync.yml'
      - 'scripts/twitch-halloween-sync.mjs'

permissions:
  contents: read

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Verify Node.js version
        run: node --version

      - name: Install Supabase client
        run: npm install --no-save @supabase/supabase-js@2

      - name: Sync Halloween Twitch clips
        env:
          TWITCH_CLIENT_ID: ${{ secrets.TWITCH_CLIENT_ID }}
          TWITCH_CLIENT_SECRET: ${{ secrets.TWITCH_CLIENT_SECRET }}
          SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
          SUPABASE_URL: https://wifkhdvmuiioisetzqfr.supabase.co
          TWITCH_LOGIN: markbeen5
        run: node scripts/twitch-halloween-sync.mjs
